<hibernate-configuration>
    <session-factory>
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/RefugioAndrea</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Configuración para mostrar y formatear SQL en consola -->
        <property name="hibernate.format_sql">true</property>

        <!-- Control sobre la creación de tablas -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapeo de entidades -->
        <mapping class="entities.Usuarios"/>
        <mapping class="entities.Animales"/>
    </session-factory>
</hibernate-configuration>
